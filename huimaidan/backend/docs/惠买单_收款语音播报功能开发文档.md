# 惠买单 - 收款语音播报功能开发文档

## 1. 功能概述

### 1.1 需求背景
商户在惠买单收款后，需要通过三木森语音播报设备实时播报"微信收款XXX元"，提升收款确认体验。

### 1.2 核心功能
1. **设备管理**：商户在管理后台绑定/解绑语音播报设备（只需设备SN）
2. **收款播报**：订单支付成功后，自动向绑定的设备推送语音播报
3. **播报日志**：记录每次播报的设备、金额、状态，便于排查

### 1.3 技术方案
- **设备通信**：通过三木森公有服务器HTTP接口 `http://cs.mqlinks.com/txmsgpush/` 推送消息
- **消息推送**：采用三木森"按key推送"方式（pushMessageByApiKey），设备按需绑定
- **异步处理**：使用ThinkPHP队列（Queue Job）异步推送播报，避免阻塞支付回调
- **依赖要求**：仅需PHP curl扩展（几乎所有环境默认开启），无需Swoole

---

## 2. 三木森API对接要点

### 2.1 通信方式
- **接口地址**：`http://cs.mqlinks.com/txmsgpush/`
- **请求方式**：POST
- **Content-Type**：`application/json`
- **消息格式**：JSON
- **编码格式**：UTF-8

### 2.2 推送方式选择
采用 **"按key推送"**（pushMessageByApiKey）方式：
- 优点：设备可随时绑定/解绑，无需关心设备在线状态
- 流程：服务器调用 `pushMessageByApiKey` → 三木森服务器转发 → 设备播报
- 播报效果：设备播报"微信收款XX元"

### 2.3 API调用流程

**Step 1：绑定设备（首次配置时调用，仅需一次）**

```
POST http://cs.mqlinks.com/txmsgpush/
Content-Type: application/json

{
    "cmd": "bindKeyReq",
    "apikey": "平台分配的密钥",
    "sn": "设备SN",
    "password": "设备密码"
}
```

响应：

```json
{
    "cmd": "bindKeyRes",
    "code": 0,
    "msg": "OK"
}
```

**Step 2：推送播报消息（日常使用，只需SN）**

```
POST http://cs.mqlinks.com/txmsgpush/
Content-Type: application/json

{
    "cmd": "pushMessageByApiKeyReq",
    "sn": "设备SN",
    "content": "微信收款12.50元"
}
```

响应：

```json
{
    "cmd": "pushMessageByApiKeyRes",
    "code": 0,
    "msg": "OK"
}
```

### 2.4 关键参数说明

| 场景 | 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| 绑定设备 | cmd | string | 是 | 固定值 `bindKeyReq` |
| 绑定设备 | apikey | string | 是 | 三木森平台分配的API密钥 |
| 绑定设备 | sn | string | 是 | 设备序列号（SN） |
| 绑定设备 | password | string | 是 | 设备密码 |
| 推送播报 | cmd | string | 是 | 固定值 `pushMessageByApiKeyReq` |
| 推送播报 | sn | string | 是 | 设备序列号（SN） |
| 推送播报 | content | string | 是 | 播报内容文本 |

> **注意**：推送播报消息时 **不需要密钥**，只需设备SN即可。

---

## 3. 数据库设计

### 3.1 新增表：语音播报设备表 `eb_merchant_voice_device`

```sql
CREATE TABLE `eb_merchant_voice_device` (
  `id` int(11) UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '设备ID',
  `mer_id` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '商户ID',
  `device_sn` varchar(64) NOT NULL DEFAULT '' COMMENT '设备SN序列号（推送播报唯一凭证）',
  `device_name` varchar(64) NOT NULL DEFAULT '' COMMENT '设备名称（用户自定义）',
  `device_type` tinyint(1) NOT NULL DEFAULT 1 COMMENT '设备类型 1=三木森语音播报器',
  `bind_status` tinyint(1) NOT NULL DEFAULT 0 COMMENT '绑定状态 0=未绑定 1=已绑定',
  `status` tinyint(1) NOT NULL DEFAULT 1 COMMENT '状态 1=启用 0=禁用',
  `last_push_time` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '最后播报时间',
  `total_push_count` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '累计播报次数',
  `remark` varchar(255) NOT NULL DEFAULT '' COMMENT '备注',
  `is_del` tinyint(1) NOT NULL DEFAULT 0 COMMENT '是否删除 0=否 1=是',
  `create_time` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '创建时间',
  `update_time` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_mer_id` (`mer_id`),
  KEY `idx_device_sn` (`device_sn`),
  KEY `idx_mer_status` (`mer_id`, `status`, `is_del`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商户语音播报设备表';
```

> **设计说明**：
> - 推送播报只需设备SN，无需密钥，因此表中不存储api_key/secret_key/token
> - `bind_status` 记录设备是否已在三木森平台完成绑定（首次使用时需调用bindKeyReq）

### 3.2 新增表：播报日志表 `eb_voice_push_log`

```sql
CREATE TABLE `eb_voice_push_log` (
  `id` int(11) UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `mer_id` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '商户ID',
  `device_id` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '设备ID',
  `device_sn` varchar(64) NOT NULL DEFAULT '' COMMENT '设备SN',
  `order_id` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '关联订单ID',
  `order_sn` varchar(32) NOT NULL DEFAULT '' COMMENT '订单号',
  `push_type` tinyint(1) NOT NULL DEFAULT 1 COMMENT '播报类型 1=收款播报 2=测试播报',
  `push_content` varchar(255) NOT NULL DEFAULT '' COMMENT '播报内容',
  `push_amount` decimal(10,2) NOT NULL DEFAULT 0.00 COMMENT '播报金额',
  `push_status` tinyint(1) NOT NULL DEFAULT 0 COMMENT '推送状态 0=待推送 1=推送中 2=成功 3=失败',
  `error_msg` varchar(255) NOT NULL DEFAULT '' COMMENT '失败原因',
  `push_time` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '推送时间',
  `create_time` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '创建时间',
  `update_time` int(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_mer_id` (`mer_id`),
  KEY `idx_device_id` (`device_id`),
  KEY `idx_order_sn` (`order_sn`),
  KEY `idx_push_status` (`push_status`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='语音播报推送日志表';
```

---

## 4. 后端开发详细设计

### 4.1 目录结构规划

```
app/
├── common/
│   ├── model/huimaidan/
│   │   ├── VoiceDevice.php              # 语音设备模型
│   │   └── VoicePushLog.php             # 播报日志模型
│   ├── dao/huimaidan/
│   │   ├── VoiceDeviceDao.php           # 设备DAO
│   │   └── VoicePushLogDao.php          # 日志DAO
│   └── repositories/huimaidan/
│       ├── VoiceDeviceRepository.php    # 设备业务仓储
│       └── VoicePushRepository.php      # 播报推送业务仓储
├── controller/merchant/huimaidan/
│   └── VoiceDevice.php                  # 商户端设备管理控制器
└── jobs/huimaidan/
    └── VoicePushJob.php                 # 播报推送队列任务

crmeb/
└── services/huimaidan/
    └── SanmusenVoiceService.php         # 三木森HTTP通信服务

route/
└── merchant/huimaidan.php               # 追加设备管理路由
```

### 4.2 Model层

#### 4.2.1 VoiceDevice.php

```php
<?php
declare(strict_types=1);

namespace app\common\model\huimaidan;

use app\common\model\BaseModel;

class VoiceDevice extends BaseModel
{
    protected $name = 'merchant_voice_device';

    // 状态常量
    const STATUS_ENABLE  = 1;
    const STATUS_DISABLE = 0;

    // 在线状态
    const ONLINE_YES = 1;
    const ONLINE_NO  = 0;

    // 设备类型
    const TYPE_SANMUSEN = 1;

    public function tablePk(): string
    {
        return 'id';
    }

    public function tableName(): string
    {
        return 'eb_merchant_voice_device';
    }

    // 关联商户
    public function merchant()
    {
        return $this->hasOne(\app\common\model\system\merchant\Merchant::class, 'mer_id', 'mer_id')
            ->field('mer_id,mer_name');
    }

    // 获取器：状态文本
    public function getStatusTextAttr($value, $data): string
    {
        $status = [
            self::STATUS_ENABLE  => '启用',
            self::STATUS_DISABLE => '禁用',
        ];
        return $status[$data['status']] ?? '未知';
    }

    // 获取器：在线状态文本
    public function getOnlineStatusTextAttr($value, $data): string
    {
        return $data['online_status'] == self::ONLINE_YES ? '在线' : '离线';
    }
}
```

#### 4.2.2 VoicePushLog.php

```php
<?php
declare(strict_types=1);

namespace app\common\model\huimaidan;

use app\common\model\BaseModel;

class VoicePushLog extends BaseModel
{
    protected $name = 'voice_push_log';

    // 推送状态
    const PUSH_STATUS_PENDING  = 0; // 待推送
    const PUSH_STATUS_PUSHING  = 1; // 推送中
    const PUSH_STATUS_SUCCESS  = 2; // 成功
    const PUSH_STATUS_FAILED   = 3; // 失败

    // 播报类型
    const PUSH_TYPE_PAY   = 1; // 收款播报
    const PUSH_TYPE_TEST  = 2; // 测试播报

    public function tablePk(): string
    {
        return 'id';
    }

    public function tableName(): string
    {
        return 'eb_voice_push_log';
    }

    // 关联设备
    public function device()
    {
        return $this->hasOne(VoiceDevice::class, 'id', 'device_id');
    }
}
```

### 4.3 DAO层

#### 4.3.1 VoiceDeviceDao.php

```php
<?php
declare(strict_types=1);

namespace app\common\dao\huimaidan;

use app\common\dao\BaseDao;
use app\common\model\huimaidan\VoiceDevice;

class VoiceDeviceDao extends BaseDao
{
    protected function getModel(): string
    {
        return VoiceDevice::class;
    }

    /**
     * 搜索条件构建
     */
    public function search(array $where = [], $field = '*', $order = '', $limit = 15)
    {
        $model = $this->getModel()::getInstance()
            ->where('is_del', 0);

        if (!empty($where['mer_id'])) {
            $model->where('mer_id', $where['mer_id']);
        }

        if (isset($where['status']) && $where['status'] !== '') {
            $model->where('status', $where['status']);
        }

        if (!empty($where['device_sn'])) {
            $model->whereLike('device_sn', '%' . $where['device_sn'] . '%');
        }

        if (!empty($where['keyword'])) {
            $model->where(function ($query) use ($where) {
                $query->whereLike('device_name', '%' . $where['keyword'] . '%')
                    ->whereOrLike('device_sn', '%' . $where['keyword'] . '%');
            });
        }

        if (!empty($where['online_status'])) {
            $model->where('online_status', $where['online_status']);
        }

        if (!empty($order)) {
            $model->order($order);
        } else {
            $model->order('id', 'desc');
        }

        if ($limit) {
            $model->limit($limit);
        }

        return $model->field($field)->select()->toArray();
    }

    /**
     * 获取商户下所有启用的设备
     */
    public function getEnabledDevices(int $merId): array
    {
        return $this->getModel()::getInstance()
            ->where('mer_id', $merId)
            ->where('status', VoiceDevice::STATUS_ENABLE)
            ->where('is_del', 0)
            ->select()
            ->toArray();
    }

    /**
     * 根据SN查找设备
     */
    public function findBySn(string $sn): ?array
    {
        return $this->getModel()::getInstance()
            ->where('device_sn', $sn)
            ->where('is_del', 0)
            ->find()
            ?->toArray();
    }

    /**
     * 统计商户设备数
     */
    public function countByMerId(int $merId): int
    {
        return $this->getModel()::getInstance()
            ->where('mer_id', $merId)
            ->where('is_del', 0)
            ->count();
    }
}
```

#### 4.3.2 VoicePushLogDao.php

```php
<?php
declare(strict_types=1);

namespace app\common\dao\huimaidan;

use app\common\dao\BaseDao;
use app\common\model\huimaidan\VoicePushLog;

class VoicePushLogDao extends BaseDao
{
    protected function getModel(): string
    {
        return VoicePushLog::class;
    }

    /**
     * 搜索条件构建
     */
    public function search(array $where = [], $field = '*', $order = '', $limit = 15)
    {
        $model = $this->getModel()::getInstance();

        if (!empty($where['mer_id'])) {
            $model->where('mer_id', $where['mer_id']);
        }

        if (!empty($where['device_id'])) {
            $model->where('device_id', $where['device_id']);
        }

        if (!empty($where['order_sn'])) {
            $model->where('order_sn', $where['order_sn']);
        }

        if (isset($where['push_status']) && $where['push_status'] !== '') {
            $model->where('push_status', $where['push_status']);
        }

        if (!empty($where['push_type'])) {
            $model->where('push_type', $where['push_type']);
        }

        if (!empty($where['start_time'])) {
            $model->where('create_time', '>=', $where['start_time']);
        }

        if (!empty($where['end_time'])) {
            $model->where('create_time', '<=', $where['end_time']);
        }

        if (!empty($order)) {
            $model->order($order);
        } else {
            $model->order('id', 'desc');
        }

        if ($limit) {
            $model->limit($limit);
        }

        return $model->field($field)->select()->toArray();
    }

    /**
     * 获取待推送的日志
     */
    public function getPendingLogs(int $limit = 10): array
    {
        return $this->getModel()::getInstance()
            ->where('push_status', VoicePushLog::PUSH_STATUS_PENDING)
            ->order('id', 'asc')
            ->limit($limit)
            ->select()
            ->toArray();
    }
}
```

### 4.4 Repository层

#### 4.4.1 VoiceDeviceRepository.php

```php
<?php
declare(strict_types=1);

namespace app\common\repositories\huimaidan;

use app\common\dao\huimaidan\VoiceDeviceDao;
use app\common\repositories\BaseRepository;
use app\exception\ValidateException;
use think\facade\Validate;

class VoiceDeviceRepository extends BaseRepository
{
    protected $dao;

    public function __construct(VoiceDeviceDao $dao)
    {
        $this->dao = $dao;
    }

    /**
     * 设备列表（分页）
     */
    public function getList(array $where, int $page, int $limit): array
    {
        $count = $this->dao->search($where, '*', '', false);
        $count = is_array($count) ? count($count) : $count;

        $list = $this->dao->search($where, '*', 'id desc', [$page, $limit]);

        return [
            'list'  => $list,
            'count' => $count,
        ];
    }

    /**
     * 新增设备
     */
    public function create(int $merId, array $data): int
    {
        // 校验
        $validate = Validate::make([
            'device_sn'   => 'require|max:64',
            'device_name' => 'require|max:64',
        ]);

        if (!$validate->check($data)) {
            throw new ValidateException($validate->getError());
        }

        // 检查SN是否已被该商户绑定
        $existing = $this->dao->getModel()::getInstance()
            ->where('mer_id', $merId)
            ->where('device_sn', $data['device_sn'])
            ->where('is_del', 0)
            ->find();

        if ($existing) {
            throw new ValidateException('该设备SN已绑定');
        }

        // 检查设备数量限制（可选）
        $count = $this->dao->countByMerId($merId);
        if ($count >= 10) {
            throw new ValidateException('最多绑定10台设备');
        }

        $id = $this->dao->create([
            'mer_id'      => $merId,
            'device_sn'   => $data['device_sn'],
            'device_name' => $data['device_name'],
            'status'      => 1,
            'create_time' => time(),
            'update_time' => time(),
        ]);

        return $id;
    }

    /**
     * 编辑设备
     */
    public function update(int $id, int $merId, array $data): bool
    {
        $device = $this->dao->get([
            'id'     => $id,
            'mer_id' => $merId,
            'is_del' => 0,
        ]);

        if (empty($device)) {
            throw new ValidateException('设备不存在');
        }

        $updateData = [];
        if (isset($data['device_name'])) {
            $updateData['device_name'] = $data['device_name'];
        }
        if (isset($data['remark'])) {
            $updateData['remark'] = $data['remark'];
        }
        $updateData['update_time'] = time();

        return $this->dao->update($id, $updateData);
    }

    /**
     * 删除设备（软删除）
     */
    public function delete(int $id, int $merId): bool
    {
        $device = $this->dao->get([
            'id'     => $id,
            'mer_id' => $merId,
            'is_del' => 0,
        ]);

        if (empty($device)) {
            throw new ValidateException('设备不存在');
        }

        return $this->dao->update($id, [
            'is_del'      => 1,
            'update_time' => time(),
        ]);
    }

    /**
     * 切换设备状态
     */
    public function changeStatus(int $id, int $merId): bool
    {
        $device = $this->dao->get([
            'id'     => $id,
            'mer_id' => $merId,
            'is_del' => 0,
        ]);

        if (empty($device)) {
            throw new ValidateException('设备不存在');
        }

        $newStatus = $device['status'] == 1 ? 0 : 1;
        return $this->dao->update($id, [
            'status'      => $newStatus,
            'update_time' => time(),
        ]);
    }

    /**
     * 获取商户下所有启用的设备
     */
    public function getEnabledDevices(int $merId): array
    {
        return $this->dao->getEnabledDevices($merId);
    }

    /**
     * 设备详情
     */
    public function detail(int $id, int $merId): array
    {
        $device = $this->dao->get([
            'id'     => $id,
            'mer_id' => $merId,
            'is_del' => 0,
        ]);

        if (empty($device)) {
            throw new ValidateException('设备不存在');
        }

        return $device;
    }
}
```

#### 4.4.2 VoicePushRepository.php

```php
<?php
declare(strict_types=1);

namespace app\common\repositories\huimaidan;

use app\common\dao\huimaidan\VoiceDeviceDao;
use app\common\dao\huimaidan\VoicePushLogDao;
use app\common\model\huimaidan\VoiceDevice;
use app\common\model\huimaidan\VoicePushLog;
use app\common\repositories\BaseRepository;
use app\exception\ValidateException;
use app\jobs\huimaidan\VoicePushJob;
use think\facade\Queue;

class VoicePushRepository extends BaseRepository
{
    protected $logDao;
    protected $deviceDao;

    public function __construct(VoicePushLogDao $logDao, VoiceDeviceDao $deviceDao)
    {
        $this->logDao    = $logDao;
        $this->deviceDao = $deviceDao;
    }

    /**
     * 创建收款播报任务
     * 在订单支付成功后调用
     *
     * @param int    $merId    商户ID
     * @param int    $orderId  订单ID
     * @param string $orderSn  订单号
     * @param float  $amount   金额
     * @return void
     */
    public function createPayBroadcast(int $merId, int $orderId, string $orderSn, float $amount): void
    {
        // 获取商户所有启用的设备
        $devices = $this->deviceDao->getEnabledDevices($merId);
        if (empty($devices)) {
            return; // 无设备，跳过
        }

        $amountStr = number_format($amount, 2);
        $content   = "微信收款{$amountStr}元";

        foreach ($devices as $device) {
            // 创建播报日志
            $logId = $this->logDao->create([
                'mer_id'       => $merId,
                'device_id'    => $device['id'],
                'device_sn'    => $device['device_sn'],
                'order_id'     => $orderId,
                'order_sn'     => $orderSn,
                'push_type'    => VoicePushLog::PUSH_TYPE_PAY,
                'push_content' => $content,
                'push_amount'  => $amount,
                'push_status'  => VoicePushLog::PUSH_STATUS_PENDING,
                'create_time'  => time(),
                'update_time'  => time(),
            ]);

            // 投递到队列异步处理
            Queue::push(VoicePushJob::class, [
                'log_id'     => $logId,
                'device_id'  => $device['id'],
            ], 'huimaidan_voice_push');
        }
    }

    /**
     * 创建测试播报任务
     */
    public function createTestBroadcast(int $merId, int $deviceId): int
    {
        $device = $this->deviceDao->get([
            'id'     => $deviceId,
            'mer_id' => $merId,
            'status' => VoiceDevice::STATUS_ENABLE,
            'is_del' => 0,
        ]);

        if (empty($device)) {
            throw new ValidateException('设备不存在或已禁用');
        }

        $content = '测试播报，收款0.01元';

        $logId = $this->logDao->create([
            'mer_id'       => $merId,
            'device_id'    => $deviceId,
            'device_sn'    => $device['device_sn'],
            'order_id'     => 0,
            'order_sn'     => '',
            'push_type'    => VoicePushLog::PUSH_TYPE_TEST,
            'push_content' => $content,
            'push_amount'  => 0.01,
            'push_status'  => VoicePushLog::PUSH_STATUS_PENDING,
            'create_time'  => time(),
            'update_time'  => time(),
        ]);

        // 同步推送测试播报
        $this->processPush($logId);

        return $logId;
    }

    /**
     * 处理播报推送
     */
    public function processPush(int $logId): bool
    {
        $log = $this->logDao->get(['id' => $logId]);
        if (empty($log) || $log['push_status'] != VoicePushLog::PUSH_STATUS_PENDING) {
            return false;
        }

        $device = $this->deviceDao->get(['id' => $log['device_id'], 'is_del' => 0]);
        if (empty($device)) {
            $this->updateLogStatus($logId, VoicePushLog::PUSH_STATUS_FAILED, '设备不存在');
            return false;
        }

        // 更新为推送中
        $this->updateLogStatus($logId, VoicePushLog::PUSH_STATUS_PUSHING);

        // 调用三木森API推送
        $service    = app()->make(\crmeb\services\huimaidan\SanmusenVoiceService::class);
        $result     = $service->pushMessage($device, $log['push_content']);
        $pushTime   = time();

        if ($result['success']) {
            $this->logDao->update($logId, [
                'push_status' => VoicePushLog::PUSH_STATUS_SUCCESS,
                'push_time'   => $pushTime,
                'update_time' => $pushTime,
            ]);

            // 更新设备最后播报时间和次数
            $this->deviceDao->update($device['id'], [
                'last_push_time'   => $pushTime,
                'total_push_count' => $device['total_push_count'] + 1,
                'update_time'      => $pushTime,
            ]);

            return true;
        } else {
            $this->updateLogStatus($logId, VoicePushLog::PUSH_STATUS_FAILED, $result['error'] ?? '推送失败');
            return false;
        }
    }

    /**
     * 更新日志状态
     */
    protected function updateLogStatus(int $logId, int $status, string $errorMsg = ''): void
    {
        $data = [
            'push_status' => $status,
            'update_time' => time(),
        ];
        if ($errorMsg) {
            $data['error_msg'] = $errorMsg;
        }
        $this->logDao->update($logId, $data);
    }

    /**
     * 获取播报日志列表
     */
    public function getLogList(array $where, int $page, int $limit): array
    {
        $count = $this->logDao->search($where, '*', '', false);
        $count = is_array($count) ? count($count) : $count;

        $list = $this->logDao->search($where, '*', 'id desc', [$page, $limit]);

        return [
            'list'  => $list,
            'count' => $count,
        ];
    }

    /**
     * 获取播报统计
     */
    public function getStatistics(int $merId): array
    {
        $todayStart = strtotime(date('Y-m-d'));
        $model      = $this->logDao->getModel()::getInstance();

        $todayCount = $model->where('mer_id', $merId)
            ->where('create_time', '>=', $todayStart)
            ->where('push_status', VoicePushLog::PUSH_STATUS_SUCCESS)
            ->count();

        $totalCount = $model->where('mer_id', $merId)
            ->where('push_status', VoicePushLog::PUSH_STATUS_SUCCESS)
            ->count();

        $failCount = $model->where('mer_id', $merId)
            ->where('push_status', VoicePushLog::PUSH_STATUS_FAILED)
            ->where('create_time', '>=', $todayStart)
            ->count();

        return [
            'today_count' => $todayCount,
            'total_count' => $totalCount,
            'fail_count'  => $failCount,
        ];
    }
}
```

### 4.5 Service层（三木森HTTP通信）

#### 4.5.1 SanmusenVoiceService.php

```php
<?php
declare(strict_types=1);

namespace crmeb\services\huimaidan;

use think\facade\Log;
use think\facade\Config;

/**
 * 三木森语音播报HTTP通信服务
 *
 * 通过三木森公有服务器HTTP接口推送消息，无需维护TCP长连接
 * 接口地址：http://cs.mqlinks.com/txmsgpush/
 */
class SanmusenVoiceService
{
    /**
     * 三木森API地址
     */
    protected string $apiUrl = 'http://cs.mqlinks.com/txmsgpush/';

    /**
     * 请求超时时间（秒）
     */
    protected int $timeout = 10;

    public function __construct()
    {
        $config = Config::get('huimaidan.voice', []);
        if (!empty($config['api_url'])) {
            $this->apiUrl = $config['api_url'];
        }
        if (!empty($config['timeout'])) {
            $this->timeout = $config['timeout'];
        }
    }

    /**
     * 推送消息到设备
     *
     * @param array  $device  设备信息（只需 device_sn）
     * @param string $message 播报内容
     * @return array ['success' => bool, 'error' => string, 'data' => array]
     */
    public function pushMessage(array $device, string $message): array
    {
        try {
            $payload = [
                'cmd'     => 'pushMessageByApiKeyReq',
                'sn'      => $device['device_sn'],
                'content' => $message,
            ];

            $response = $this->httpPost($payload);

            if ($response === false) {
                return ['success' => false, 'error' => '请求三木森服务器失败'];
            }

            $data = json_decode($response, true);
            if (json_last_error() !== JSON_ERROR_NONE) {
                return ['success' => false, 'error' => '响应解析失败: ' . $response];
            }

            if (($data['cmd'] ?? '') !== 'pushMessageByApiKeyRes') {
                return ['success' => false, 'error' => '响应异常: ' . ($data['msg'] ?? $response)];
            }

            if (($data['code'] ?? 0) != 0) {
                return ['success' => false, 'error' => '推送失败: ' . ($data['msg'] ?? '未知错误')];
            }

            return ['success' => true, 'data' => $data];
        } catch (\Throwable $e) {
            Log::error('三木森语音播报推送异常: ' . $e->getMessage());
            return ['success' => false, 'error' => $e->getMessage()];
        }
    }

    /**
     * 绑定设备（首次配置时调用，仅需一次）
     *
     * @param string $sn       设备SN
     * @param string $apiKey   三木森平台分配的API密钥
     * @param string $password 设备密码
     * @return array ['success' => bool, 'error' => string, 'data' => array]
     */
    public function bindDevice(string $sn, string $apiKey, string $password): array
    {
        try {
            $payload = [
                'cmd'      => 'bindKeyReq',
                'apikey'   => $apiKey,
                'sn'       => $sn,
                'password' => $password,
            ];

            $response = $this->httpPost($payload);

            if ($response === false) {
                return ['success' => false, 'error' => '请求三木森服务器失败'];
            }

            $data = json_decode($response, true);
            if (json_last_error() !== JSON_ERROR_NONE) {
                return ['success' => false, 'error' => '响应解析失败: ' . $response];
            }

            if (($data['cmd'] ?? '') !== 'bindKeyRes') {
                return ['success' => false, 'error' => '响应异常: ' . ($data['msg'] ?? $response)];
            }

            if (($data['code'] ?? 0) != 0) {
                return ['success' => false, 'error' => '绑定失败: ' . ($data['msg'] ?? '未知错误')];
            }

            return ['success' => true, 'data' => $data];
        } catch (\Throwable $e) {
            Log::error('三木森设备绑定异常: ' . $e->getMessage());
            return ['success' => false, 'error' => $e->getMessage()];
        }
    }

    /**
     * 发送HTTP POST请求
     *
     * @param array $payload 请求数据
     * @return string|false 响应内容，失败返回false
     */
    protected function httpPost(array $payload)
    {
        $json = json_encode($payload, JSON_UNESCAPED_UNICODE);
        if ($json === false) {
            Log::error('三木森请求数据编码失败');
            return false;
        }

        $ch = curl_init($this->apiUrl);
        curl_setopt_array($ch, [
            CURLOPT_POST           => true,
            CURLOPT_POSTFIELDS     => $json,
            CURLOPT_RETURNTRANSFER => true,
            CURLOPT_TIMEOUT        => $this->timeout,
            CURLOPT_CONNECTTIMEOUT => $this->timeout,
            CURLOPT_HTTPHEADER     => [
                'Content-Type: application/json',
                'Content-Length: ' . strlen($json),
            ],
            // HTTP接口无需SSL验证
            CURLOPT_SSL_VERIFYPEER => false,
            CURLOPT_SSL_VERIFYHOST => false,
        ]);

        $response = curl_exec($ch);
        $errno    = curl_errno($ch);
        $error    = curl_error($ch);
        $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        curl_close($ch);

        if ($errno !== 0) {
            Log::error("三木森API请求失败: [{$errno}] {$error}");
            return false;
        }

        if ($httpCode !== 200) {
            Log::error("三木森API返回异常HTTP状态码: {$httpCode}");
            return false;
        }

        return $response;
    }

    /**
     * 测试设备连通性
     *
     * @param array $device 设备信息
     * @return array
     */
    public function testConnection(array $device): array
    {
        return $this->pushMessage($device, '测试播报，设备连接正常');
    }
}
```

### 4.6 Controller层

#### 4.6.1 商户端控制器 VoiceDevice.php

```php
<?php
declare(strict_types=1);

namespace app\controller\merchant\huimaidan;

use app\controller\merchant\AuthController;
use app\common\repositories\huimaidan\VoiceDeviceRepository;
use app\common\repositories\huimaidan\VoicePushRepository;

class VoiceDevice extends AuthController
{
    protected $voiceDeviceRepository;
    protected $voicePushRepository;

    public function __construct(
        VoiceDeviceRepository $voiceDeviceRepository,
        VoicePushRepository $voicePushRepository
    ) {
        $this->voiceDeviceRepository = $voiceDeviceRepository;
        $this->voicePushRepository   = $voicePushRepository;
    }

    /**
     * 设备列表
     * GET /merchant/huimaidan/voice_device/list
     */
    public function lst()
    {
        $page    = $this->request->get('page', 1);
        $limit   = $this->request->get('limit', 15);
        $keyword = $this->request->get('keyword', '');

        $where = ['mer_id' => $this->merId];
        if ($keyword) {
            $where['keyword'] = $keyword;
        }

        $result = $this->voiceDeviceRepository->getList($where, (int)$page, (int)$limit);

        return app('json')->success($result);
    }

    /**
     * 设备详情
     * GET /merchant/huimaidan/voice_device/detail
     */
    public function detail()
    {
        $id = $this->request->get('id', 0);
        if (!$id) {
            return app('json')->fail('参数错误');
        }

        $result = $this->voiceDeviceRepository->detail((int)$id, $this->merId);
        return app('json')->success($result);
    }

    /**
     * 新增设备
     * POST /merchant/huimaidan/voice_device/create
     */
    public function create()
    {
        $data = [
            'device_sn'   => $this->request->post('device_sn', ''),
            'device_name' => $this->request->post('device_name', ''),
        ];

        $id = $this->voiceDeviceRepository->create($this->merId, $data);
        return app('json')->success('绑定成功', ['id' => $id]);
    }

    /**
     * 编辑设备
     * POST /merchant/huimaidan/voice_device/update
     */
    public function update()
    {
        $id   = $this->request->post('id', 0);
        $data = [
            'device_name' => $this->request->post('device_name', ''),
            'remark'      => $this->request->post('remark', ''),
        ];

        $this->voiceDeviceRepository->update((int)$id, $this->merId, $data);
        return app('json')->success('编辑成功');
    }

    /**
     * 删除设备
     * POST /merchant/huimaidan/voice_device/delete
     */
    public function delete()
    {
        $id = $this->request->post('id', 0);
        $this->voiceDeviceRepository->delete((int)$id, $this->merId);
        return app('json')->success('删除成功');
    }

    /**
     * 切换设备状态
     * POST /merchant/huimaidan/voice_device/changeStatus
     */
    public function changeStatus()
    {
        $id = $this->request->post('id', 0);
        $this->voiceDeviceRepository->changeStatus((int)$id, $this->merId);
        return app('json')->success('操作成功');
    }

    /**
     * 测试播报
     * POST /merchant/huimaidan/voice_device/testPush
     */
    public function testPush()
    {
        $deviceId = $this->request->post('device_id', 0);
        $logId    = $this->voicePushRepository->createTestBroadcast($this->merId, (int)$deviceId);
        return app('json')->success('测试播报已发送', ['log_id' => $logId]);
    }

    /**
     * 播报日志列表
     * GET /merchant/huimaidan/voice_device/pushLog
     */
    public function pushLog()
    {
        $page  = $this->request->get('page', 1);
        $limit = $this->request->get('limit', 15);
        $where = ['mer_id' => $this->merId];

        $deviceId = $this->request->get('device_id', 0);
        if ($deviceId) {
            $where['device_id'] = (int)$deviceId;
        }

        $pushStatus = $this->request->get('push_status', '');
        if ($pushStatus !== '') {
            $where['push_status'] = (int)$pushStatus;
        }

        $result = $this->voicePushRepository->getLogList($where, (int)$page, (int)$limit);
        return app('json')->success($result);
    }

    /**
     * 播报统计
     * GET /merchant/huimaidan/voice_device/statistics
     */
    public function statistics()
    {
        $result = $this->voicePushRepository->getStatistics($this->merId);
        return app('json')->success($result);
    }
}
```

### 4.7 Job层（队列任务）

#### 4.7.1 VoicePushJob.php

```php
<?php
declare(strict_types=1);

namespace app\jobs\huimaidan;

use app\common\repositories\huimaidan\VoicePushRepository;
use think\queue\Job;

class VoicePushJob
{
    /**
     * 任务执行
     */
    public function fire(Job $job, array $data): void
    {
        $logId = $data['log_id'] ?? 0;
        if (!$logId) {
            $job->delete();
            return;
        }

        try {
            $repository = app()->make(VoicePushRepository::class);
            $result     = $repository->processPush($logId);

            if ($result) {
                $job->delete();
            } else {
                // 失败后重试
                $this->retryJob($job, $data);
            }
        } catch (\Throwable $e) {
            $this->retryJob($job, $data);
        }
    }

    /**
     * 任务失败处理
     */
    public function failed(\Throwable $exception): void
    {
        \think\facade\Log::error('语音播报任务异常: ' . $exception->getMessage());
    }

    /**
     * 重试任务
     */
    protected function retryJob(Job $job, array $data): void
    {
        $job->release(30); // 30秒后重试
    }
}
```

### 4.8 路由配置

#### 4.8.1 route/merchant/huimaidan.php 追加路由

```php
<?php
// +----------------------------------------------------------------------
// | 惠买单 - 语音播报设备管理路由
// +----------------------------------------------------------------------

Route::group('huimaidan', function () {

    // 语音播报设备管理
    Route::group('voice_device', function () {
        Route::get('lst', 'huimaidan.VoiceDevice/lst');
        Route::get('detail', 'huimaidan.VoiceDevice/detail');
        Route::post('create', 'huimaidan.VoiceDevice/create');
        Route::post('update', 'huimaidan.VoiceDevice/update');
        Route::post('delete', 'huimaidan.VoiceDevice/delete');
        Route::post('changeStatus', 'huimaidan.VoiceDevice/changeStatus');
        Route::post('testPush', 'huimaidan.VoiceDevice/testPush');
        Route::get('pushLog', 'huimaidan.VoiceDevice/pushLog');
        Route::get('statistics', 'huimaidan.VoiceDevice/statistics');
    })->middleware(\app\common\middleware\MerchantCheckBaseInfoMiddleware::class);

})->middleware(\app\common\middleware\MerchantTokenMiddleware::class)
  ->middleware(\app\common\middleware\MerchantAuthMiddleware::class)
  ->allowCrossDomain();
```

### 4.9 支付回调集成

在 `app/common/repositories/huimaidan/OrderRepository.php` 的 `paySuccess()` 方法中，支付成功后追加语音播报调用：

```php
// 在 paySuccess() 方法末尾，syncPaySuccessRewards() 之后追加

// 触发语音播报
$this->broadcastVoice($orderData);
```

新增方法：

```php
/**
 * 触发收款语音播报
 */
protected function broadcastVoice(array $orderData): void
{
    try {
        $voicePushRepo = app()->make(\app\common\repositories\huimaidan\VoicePushRepository::class);
        $voicePushRepo->createPayBroadcast(
            $orderData['mer_id'],
            $orderData['order_id'],
            $orderData['order_sn'],
            $orderData['pay_price'] ?? $orderData['total_price']
        );
    } catch (\Throwable $e) {
        \think\facade\Log::error('语音播报任务创建失败: ' . $e->getMessage());
    }
}
```

---

## 5. 前端管理端开发

### 5.1 商户后台 - 语音播报设备管理页面

> 注意：前端为编译后的Vue + ElementUI，需要在前端源码仓库中开发。

#### 5.1.1 页面组件：VoiceDevice.vue

**功能模块：**

1. **设备列表**
   - 表格展示：设备名称、SN、状态、在线状态、最后播报时间、累计播报次数、操作
   - 搜索：设备名称/SN关键词
   - 操作：编辑、启用/禁用、测试播报、删除

2. **新增/编辑设备弹窗**
   - 设备SN（必填）
   - 设备名称（必填）
   - 备注

3. **播报日志Tab**
   - 筛选：设备、推送状态、时间范围
   - 表格：播报内容、金额、状态、时间

4. **统计卡片**
   - 今日播报次数
   - 累计播报次数
   - 今日失败次数

#### 5.1.2 API接口对接

```javascript
// api/voiceDevice.js
import request from '@/libs/request'

export function getVoiceDeviceList(params) {
  return request.get('huimaidan/voice_device/lst', params)
}

export function getVoiceDeviceDetail(params) {
  return request.get('huimaidan/voice_device/detail', params)
}

export function createVoiceDevice(data) {
  return request.post('huimaidan/voice_device/create', data)
}

export function updateVoiceDevice(data) {
  return request.post('huimaidan/voice_device/update', data)
}

export function deleteVoiceDevice(data) {
  return request.post('huimaidan/voice_device/delete', data)
}

export function changeVoiceDeviceStatus(data) {
  return request.post('huimaidan/voice_device/changeStatus', data)
}

export function testVoicePush(data) {
  return request.post('huimaidan/voice_device/testPush', data)
}

export function getVoicePushLog(params) {
  return request.get('huimaidan/voice_device/pushLog', params)
}

export function getVoiceStatistics(params) {
  return request.get('huimaidan/voice_device/statistics', params)
}
```

---

## 6. 开发任务清单

### 6.1 后端开发

| 序号 | 任务 | 优先级 | 预估工时 |
|------|------|--------|----------|
| 1 | 执行SQL创建数据库表 | P0 | 0.5h |
| 2 | 创建Model层（VoiceDevice、VoicePushLog） | P0 | 1h |
| 3 | 创建DAO层（VoiceDeviceDao、VoicePushLogDao） | P0 | 1h |
| 4 | 创建Repository层（VoiceDeviceRepository、VoicePushRepository） | P0 | 3h |
| 5 | 创建SanmusenVoiceService（三木森HTTP通信） | P0 | 2h |
| 6 | 创建VoicePushJob（队列任务） | P0 | 1h |
| 7 | 创建商户端Controller（VoiceDevice） | P0 | 2h |
| 8 | 添加路由配置 | P0 | 0.5h |
| 9 | 支付回调集成（OrderRepository追加） | P0 | 1h |
| 10 | 配置ThinkPHP队列驱动 | P1 | 0.5h |

**后端合计：约12.5小时**

### 6.2 前端开发

| 序号 | 任务 | 优先级 | 预估工时 |
|------|------|--------|----------|
| 1 | 设备管理列表页面 | P0 | 4h |
| 2 | 新增/编辑设备弹窗 | P0 | 2h |
| 3 | 播报日志页面 | P0 | 3h |
| 4 | 统计卡片组件 | P1 | 1h |
| 5 | API接口封装 | P0 | 1h |

**前端合计：约11小时**

### 6.3 测试

| 序号 | 任务 | 优先级 | 预估工时 |
|------|------|--------|----------|
| 1 | 设备CRUD功能测试 | P0 | 2h |
| 2 | 三木森HTTP接口联调 | P0 | 2h |
| 3 | 支付回调播报集成测试 | P0 | 3h |
| 4 | 队列消费测试 | P0 | 2h |
| 5 | 异常场景测试（设备离线、网络断开等） | P1 | 2h |

**测试合计：约11小时**

---

## 7. 部署注意事项

### 7.1 环境要求
- PHP版本 >= 7.2
- PHP curl扩展（默认已开启，`php -m | grep curl` 检查）
- 无需Swoole扩展

### 7.2 队列服务
- 配置ThinkPHP队列驱动（Redis推荐）
- 启动队列消费者：`php think queue:work --queue=huimaidan_voice_push`
- 建议使用Supervisor守护队列进程

### 7.3 网络要求
- 服务器需要能访问 `http://cs.mqlinks.com/txmsgpush/`（HTTP）
- 防火墙需开放出站HTTP 80端口（或HTTPS 443端口）

### 7.4 配置项（config/huimaidan.php）

```php
<?php
return [
    // 三木森语音播报配置
    'voice' => [
        'api_url'    => 'http://cs.mqlinks.com/txmsgpush/',
        'timeout'    => 10,  // 请求超时（秒）
        'max_retry'  => 3,   // 最大重试次数
        'retry_delay' => 30, // 重试间隔（秒）
    ],
];
```

---

## 8. 时序图

### 8.1 设备绑定流程

```
商户后台                    后端服务                   三木森HTTP服务器
  |                          |                          |
  |--- 绑定设备请求 -------->|                          |
  |   (SN, API Key, 密码)   |                          |
  |                          |--- HTTP POST ----------->|
  |                          |   bindKeyReq             |
  |                          |<-- HTTP Response --------|
  |                          |   bindKeyRes (code=0)    |
  |                          |                          |
  |                          |--- 保存设备信息 -------->|
  |<-- 绑定成功 ------------|                          |
```

### 8.2 收款播报流程

```
用户支付成功               支付回调                  后端服务                队列               三木森HTTP服务器
  |                          |                        |                     |                    |
  |--- 支付成功回调 -------->|                        |                     |                    |
  |                          |--- paySuccess() ------>|                     |                    |
  |                          |                        |--- 创建播报任务 --->|                    |
  |                          |                        |                     |--- 消费任务 ------>|
  |                          |                        |                     |--- HTTP POST ----->|
  |                          |                        |                     |  pushMessageByApiKey|
  |                          |                        |                     |<-- HTTP Response --|
  |                          |                        |                     |                    |
  |                          |                        |                     |              设备播报:
  |                          |                        |                     |         "微信收款XX元"
```

---

## 9. 扩展建议

### 9.1 多设备支持
- 当前设计已支持一个商户绑定多台设备
- 所有启用设备会同时播报
- 可按需增加"主设备/副设备"区分

### 9.2 播报模板配置
- 可扩展支持自定义播报模板
- 如："支付宝收款XXX元"、"微信收款XXX元，优惠XX元"
- 在系统配置中增加播报模板字段

### 9.3 设备心跳检测
- 可增加定时任务检测设备在线状态
- 通过 `getOnlineDevices` API获取在线设备列表
- 在管理端展示设备在线状态

### 9.4 播报音量/语速
- 可通过 `setVolume` 命令调整设备音量
- 可通过 `setTtsType` 命令调整语音类型
